# Changes Index

恢复索引。会话开始时先读本文件，找到 `active` 变更后读其 `summary.md`。

## Registry

| Change | Status | Resume point | Notes |
|--------|--------|--------------|-------|


## 说明

状态定义（`active`/`done`/`abandoned`）与维护规则见 `.harness/changes/structure.md`。
本文件只承载 Registry 表；任意时刻最多一个 `active`。
